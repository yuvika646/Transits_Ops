# TransitOps

Frontend-only smart transport operations platform built with Next.js and strict TypeScript.

## Setup

`npm install` then `npm run dev`. Validate with `npm run typecheck`, `npm run lint`, `npm run test`, `npm run test:e2e`, and `npm run build`.


